{
  "name": "aceros-temuco-atm-agent-demo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/dashboard",
    "packages/shared-types"
  ],
  "scripts": {
    "dashboard:dev": "npm run dev --workspace=apps/dashboard",
    "dashboard:build": "npm run build --workspace=apps/dashboard",
    "dashboard:start": "npm run start --workspace=apps/dashboard",
    "types:build": "npm run build --workspace=packages/shared-types"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
